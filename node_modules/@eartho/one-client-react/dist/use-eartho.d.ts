/// <reference types="react" />
import { User } from '@eartho/one-client-js';
import { EarthoOneContextInterface } from './eartho-context';
/**
 * ```js
 * const {
 *   // Auth state:
 *   error,
 *   isConnected,
 *   isLoading,
 *   user,
 *   // Auth methods:
 *   getAccessTokenSilently,
 *   getAccessTokenWithPopup,
 *   getIdToken,
 *   connectWithRedirect,
 *   connectWithPopup,
 *   logout,
 * } = useEarthoOne<TUser>();
 * ```
 *
 * Use the `useEarthoOne` hook in your components to access the auth state and methods.
 *
 * TUser is an optional type param to provide a type to the `user` field.
 */
declare const useEarthoOne: <TUser extends User = User>(context?: import("react").Context<EarthoOneContextInterface<User>>) => EarthoOneContextInterface<TUser>;
export default useEarthoOne;
//# sourceMappingURL=use-eartho.d.ts.map